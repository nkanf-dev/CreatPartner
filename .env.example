# CreatPartner 环境配置文件
# 复制此文件为 .env 并填入你的API密钥

# =================================
# 必需配置 (Required)
# =================================

# LLM服务提供商配置 (必需 - 选择其中一个)
# 硅基流动 (推荐) - 成本低，速度快
SILICONFLOW_API_KEY=your_siliconflow_api_key_here

# 或者使用 OpenAI
# OPENAI_API_KEY=your_openai_api_key_here

# 或者使用 DeepSeek
# DEEPSEEK_API_KEY=your_deepseek_api_key_here

# =================================
# 数据库配置 (Database)
# =================================

# MongoDB连接字符串 (默认本地MongoDB)
MONGODB_URI=mongodb://localhost:27017

# 数据库名称 (可选)
DB_NAME=creatpartner

# =================================
# 搜索服务配置 (Optional)
# =================================

# Jina AI API密钥
# 免费获取: https://jina.ai/?sui=apikey
JINA_API_KEY=your_jina_api_key_here

# =================================
# 系统配置 (System Settings)
# =================================

# LLM提供商选择 (siliconflow/openai/deepseek)
LLM_PROVIDER=siliconflow

# 默认模型名称
LLM_MODEL=Qwen/Qwen2.5-7B-Instruct

# 调试模式 (true/false)
DEBUG=true

# =================================
# 性能配置 (Performance)
# =================================

# 最大搜索结果数量
MAX_SEARCH_RESULTS=5

# 搜索请求超时 (秒)
SEARCH_TIMEOUT=30

# 启用缓存 (true/false)
ENABLE_CACHING=true

# 缓存过期时间 (秒)
CACHE_TTL=300

# =================================
# 项目配置 (Project Settings)
# =================================

# 每个会话最大请求数 (已移除限制)
MAX_REQUESTS_PER_SESSION=9999

# 每个会话最大Token数 (已移除限制)
MAX_TOKENS_PER_SESSION=999999

# 每个会话最大工具调用数 (已移除限制)
MAX_TOOL_CALLS_PER_SESSION=9999

# 慢操作阈值 (秒)
SLOW_OPERATION_THRESHOLD=3.0

# =================================
# 使用说明
# =================================

# 1. 必需服务:
#    - LLM API (硅基流动/OpenAI/DeepSeek)
#    - MongoDB (本地或云端)
#    - Jina AI (增强搜索和向量化)
#
# 3. 快速开始:
#    - 复制为 .env 文件
#    - 填入 SILICONFLOW_API_KEY 
#    - 启动 MongoDB
#    - 运行: uv run streamlit run app.py
